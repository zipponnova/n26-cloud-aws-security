provider "aws" {
  region = "us-west-2" # Replace with your desired AWS region
  # Optionally, you can specify access_key and secret_key here, 
  # but it's better to use environment variables or shared credentials.
}
